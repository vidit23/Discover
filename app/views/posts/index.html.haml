- @posts.each do |post|
  = link_to (image_tag post.image.url(:small)), post
  %h2= link_to post.title, post
  %p
    = post.comments.count
    Comments
  %p
    = post.get_likes.size
    Likes
  %p
    = post.get_dislikes.size
    Dislikes

= link_to "Add New Artwork", new_post_path
